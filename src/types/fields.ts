export interface ConfigData {
    name : string
    integer : number
    float : number
    date : string
    datetime : string
    duration : number
    point : [number, number]
    extent : [number, number, number, number]
    select : string
    combobox : string
}